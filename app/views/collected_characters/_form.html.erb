<%= form_for(@collected_character) do |f| %>
  <% if @collected_character.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@collected_character.errors.count, "error") %> prohibited this collected_character from being saved:</h2>

      <ul>
      <% @collected_character.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :character_id %><br />
    <%= f.number_field :character_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
