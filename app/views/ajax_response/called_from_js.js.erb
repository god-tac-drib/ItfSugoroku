//この内容は、実行結果ではなくそのまま文字列として返る。なので返った先でこれをeval()に入れて実際に実行してやって実行結果の値を取り出す

function response() {
  return "<%=j @response.html_safe %>"; // html_safeメソッドを叩いておかないと、クオーテーションが&quotにエスケープされてしまう。これではjsonパースできないのできちんと叩いておく
}

response();